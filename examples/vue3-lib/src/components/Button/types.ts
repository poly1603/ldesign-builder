export interface ButtonProps {
  /**
   * 按钮类型
   */
  type?: 'primary' | 'default' | 'danger'
  
  /**
   * 按钮大小
   */
  size?: 'small' | 'medium' | 'large'
  
  /**
   * 是否禁用
   */
  disabled?: boolean
}

